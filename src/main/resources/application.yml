server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/saeidzz/sample-config-git-resource.git
          search-paths:
            - station*
          repos:
            perf:
              pattern: '*/perf'
              uri: https://github.com/saeidzz/sample-config-git-resource-.git
              search-paths:
                  - station*